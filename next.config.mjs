/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['raw.githubusercontent.com', 'newsapi.org', 'www.tbstat.com', 'image.cnbcfm.com'],
  }
};

export default nextConfig;
